File Path: modules/summaries.js
Summary:
- Reads and returns the contents of all .ai.txt files in a directory
- Uses fast-glob to ignore specified patterns
- Throws an error if no matching files are found
- Uses fs module to read file contents
- Exports readAllSummaries and getFiles functions, as well as types object